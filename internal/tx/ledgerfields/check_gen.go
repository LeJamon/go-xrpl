// Code generated by ledgerfieldsgen; DO NOT EDIT.
//
// Source: internal/tx/ledgerfields/spec/spec.go
// Regenerate: go generate ./internal/tx/ledgerfields/...

package ledgerfields

import (
	"github.com/LeJamon/go-xrpl/codec/binarycodec"
	"github.com/LeJamon/go-xrpl/crypto/common"
	"github.com/LeJamon/go-xrpl/protocol"
)

func init() {
	Register("Check", func() Entry { return new(Check) })
}

// Check is the typed representation of a Check ledger entry.
// The present bitset tracks which fields appear on the decoded blob so the
// emit methods only write entries that actually exist. The struct carries
// every on-wire field — including those excluded from metadata
// (sMD_Never) — so Decode → Encode is byte-identical.
type Check struct {
	present           uint64
	Account           string // AccountID (base58)
	Destination       string // AccountID (base58)
	SendMax           any    // Amount (XRP string | IOU map)
	Sequence          uint32
	OwnerNode         string // UInt64 (lowercase hex, no leading zeros)
	DestinationNode   string // UInt64 (lowercase hex, no leading zeros)
	Expiration        uint32
	InvoiceID         string // Hash256 (uppercase hex)
	SourceTag         uint32
	DestinationTag    uint32
	Flags             uint32
	PreviousTxnID     string // Hash256 (uppercase hex)
	PreviousTxnLgrSeq uint32
}

const (
	checkBitAccount uint64 = 1 << iota
	checkBitDestination
	checkBitSendMax
	checkBitSequence
	checkBitOwnerNode
	checkBitDestinationNode
	checkBitExpiration
	checkBitInvoiceID
	checkBitSourceTag
	checkBitDestinationTag
	checkBitFlags
	checkBitPreviousTxnID
	checkBitPreviousTxnLgrSeq
)

// Decode populates the struct from binary ledger-entry data via a streaming
// reader. Unknown / sMD_Never fields are skipped without allocation.
func (c *Check) Decode(data []byte) error {
	*c = Check{}
	sr := newStreamReader(data)
	for sr.hasMore() {
		typeCode, fieldCode, err := sr.readFieldHeader()
		if err != nil {
			return err
		}
		switch typeCode {
		case 1: // UInt16
			u16Val, err := sr.readUint16()
			if err != nil {
				return err
			}
			val := int(u16Val)
			switch fieldCode {
			case 1:
				_ = val // synthetic LedgerEntryType; discard
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		case 2: // UInt32
			val, err := sr.readUint32()
			if err != nil {
				return err
			}
			switch fieldCode {
			case 2:
				c.Flags = val
				c.present |= checkBitFlags
			case 3:
				c.SourceTag = val
				c.present |= checkBitSourceTag
			case 4:
				c.Sequence = val
				c.present |= checkBitSequence
			case 5:
				c.PreviousTxnLgrSeq = val
				c.present |= checkBitPreviousTxnLgrSeq
			case 10:
				c.Expiration = val
				c.present |= checkBitExpiration
			case 14:
				c.DestinationTag = val
				c.present |= checkBitDestinationTag
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		case 3: // UInt64
			switch fieldCode {
			case 4:
				val, err := sr.readUint64Hex()
				if err != nil {
					return err
				}
				c.OwnerNode = val
				c.present |= checkBitOwnerNode
			case 9:
				val, err := sr.readUint64Hex()
				if err != nil {
					return err
				}
				c.DestinationNode = val
				c.present |= checkBitDestinationNode
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		case 5: // Hash256
			val, err := sr.readHash(32)
			if err != nil {
				return err
			}
			switch fieldCode {
			case 5:
				c.PreviousTxnID = val
				c.present |= checkBitPreviousTxnID
			case 17:
				c.InvoiceID = val
				c.present |= checkBitInvoiceID
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		case 6: // Amount
			val, err := sr.readAmountAny()
			if err != nil {
				return err
			}
			switch fieldCode {
			case 9:
				c.SendMax = val
				c.present |= checkBitSendMax
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		case 8: // AccountID
			val, err := sr.readAccountID()
			if err != nil {
				return err
			}
			switch fieldCode {
			case 1:
				c.Account = val
				c.present |= checkBitAccount
			case 3:
				c.Destination = val
				c.present |= checkBitDestination
			default:
				return newErrUnknownField("Check", typeCode, fieldCode)
			}
		default:
			return newErrUnknownField("Check", typeCode, fieldCode)
		}
	}
	return nil
}

// emitAll writes every present default-meta field. skipDefault filters the
// "zero" value for CreatedNode.NewFields to match rippled, which omits
// defaulted fields from NewFields.
func (c *Check) emitAll(out map[string]any, skipDefault bool) {
	if c.present&checkBitAccount != 0 && !(skipDefault && c.Account == "") {
		out["Account"] = c.Account
	}
	if c.present&checkBitDestination != 0 && !(skipDefault && c.Destination == "") {
		out["Destination"] = c.Destination
	}
	if c.present&checkBitSendMax != 0 {
		out["SendMax"] = c.SendMax
	}
	if c.present&checkBitSequence != 0 && !(skipDefault && c.Sequence == 0) {
		out["Sequence"] = c.Sequence
	}
	if c.present&checkBitOwnerNode != 0 && !(skipDefault && isZeroHexString(c.OwnerNode)) {
		out["OwnerNode"] = c.OwnerNode
	}
	if c.present&checkBitDestinationNode != 0 && !(skipDefault && isZeroHexString(c.DestinationNode)) {
		out["DestinationNode"] = c.DestinationNode
	}
	if c.present&checkBitExpiration != 0 && !(skipDefault && c.Expiration == 0) {
		out["Expiration"] = c.Expiration
	}
	if c.present&checkBitInvoiceID != 0 && !(skipDefault && isZeroHexString(c.InvoiceID)) {
		out["InvoiceID"] = c.InvoiceID
	}
	if c.present&checkBitSourceTag != 0 && !(skipDefault && c.SourceTag == 0) {
		out["SourceTag"] = c.SourceTag
	}
	if c.present&checkBitDestinationTag != 0 && !(skipDefault && c.DestinationTag == 0) {
		out["DestinationTag"] = c.DestinationTag
	}
	if c.present&checkBitFlags != 0 && !(skipDefault && c.Flags == 0) {
		out["Flags"] = c.Flags
	}
}

// EmitNewFields emits fields for a CreatedNode (sMD_Create | sMD_Always),
// filtering out default values to match rippled.
func (c *Check) EmitNewFields(out map[string]any) {
	c.emitAll(out, true)
}

// EmitFinalFields emits fields for ModifiedNode.FinalFields (sMD_Always |
// sMD_ChangeNew), no default-value filter.
func (c *Check) EmitFinalFields(out map[string]any) {
	c.emitAll(out, false)
}

// EmitPreviousFields emits the original values of fields that changed
// between prev and the receiver (sMD_ChangeOrig — MetaDefault only).
func (c *Check) EmitPreviousFields(prev Entry, out map[string]any) {
	p, ok := prev.(*Check)
	if !ok || p == nil {
		return
	}
	emitIfChangedString(out, "Account", p.Account, c.Account, p.present&checkBitAccount, c.present&checkBitAccount)
	emitIfChangedString(out, "Destination", p.Destination, c.Destination, p.present&checkBitDestination, c.present&checkBitDestination)
	emitIfChangedAmount(out, "SendMax", p.SendMax, c.SendMax, p.present&checkBitSendMax, c.present&checkBitSendMax)
	emitIfChangedUint32(out, "Sequence", p.Sequence, c.Sequence, p.present&checkBitSequence, c.present&checkBitSequence)
	emitIfChangedString(out, "OwnerNode", p.OwnerNode, c.OwnerNode, p.present&checkBitOwnerNode, c.present&checkBitOwnerNode)
	emitIfChangedString(out, "DestinationNode", p.DestinationNode, c.DestinationNode, p.present&checkBitDestinationNode, c.present&checkBitDestinationNode)
	emitIfChangedUint32(out, "Expiration", p.Expiration, c.Expiration, p.present&checkBitExpiration, c.present&checkBitExpiration)
	emitIfChangedString(out, "InvoiceID", p.InvoiceID, c.InvoiceID, p.present&checkBitInvoiceID, c.present&checkBitInvoiceID)
	emitIfChangedUint32(out, "SourceTag", p.SourceTag, c.SourceTag, p.present&checkBitSourceTag, c.present&checkBitSourceTag)
	emitIfChangedUint32(out, "DestinationTag", p.DestinationTag, c.DestinationTag, p.present&checkBitDestinationTag, c.present&checkBitDestinationTag)
	emitIfChangedUint32(out, "Flags", p.Flags, c.Flags, p.present&checkBitFlags, c.present&checkBitFlags)
}

// EmitChangeOrigFields writes the names of every present field carrying
// sMD_ChangeOrig (MetaDefault). The empty-PreviousFields heuristic uses
// this to scope its orig-vs-cur presence comparison so MetaAlways fields
// (which appear in FinalFields but lack sMD_ChangeOrig at the rippled
// level) cannot trip a spurious STI_NOTPRESENT emission.
func (c *Check) EmitChangeOrigFields(out map[string]any) {
	if c.present&checkBitAccount != 0 {
		out["Account"] = c.Account
	}
	if c.present&checkBitDestination != 0 {
		out["Destination"] = c.Destination
	}
	if c.present&checkBitSendMax != 0 {
		out["SendMax"] = c.SendMax
	}
	if c.present&checkBitSequence != 0 {
		out["Sequence"] = c.Sequence
	}
	if c.present&checkBitOwnerNode != 0 {
		out["OwnerNode"] = c.OwnerNode
	}
	if c.present&checkBitDestinationNode != 0 {
		out["DestinationNode"] = c.DestinationNode
	}
	if c.present&checkBitExpiration != 0 {
		out["Expiration"] = c.Expiration
	}
	if c.present&checkBitInvoiceID != 0 {
		out["InvoiceID"] = c.InvoiceID
	}
	if c.present&checkBitSourceTag != 0 {
		out["SourceTag"] = c.SourceTag
	}
	if c.present&checkBitDestinationTag != 0 {
		out["DestinationTag"] = c.DestinationTag
	}
	if c.present&checkBitFlags != 0 {
		out["Flags"] = c.Flags
	}
}

// EmitDeleteFinalFields emits fields for DeletedNode.FinalFields
// (sMD_Always | sMD_DeleteFinal), including PreviousTxn* which are
// otherwise hidden.
func (c *Check) EmitDeleteFinalFields(out map[string]any) {
	c.emitAll(out, false)
	if c.present&checkBitPreviousTxnID != 0 {
		out["PreviousTxnID"] = c.PreviousTxnID
	}
	if c.present&checkBitPreviousTxnLgrSeq != 0 {
		out["PreviousTxnLgrSeq"] = c.PreviousTxnLgrSeq
	}
}

func (c *Check) EmitDeletePreviousFields(prev Entry, out map[string]any) {
	c.EmitPreviousFields(prev, out)
}

// PreviousTxn returns the threading values from the receiver. Empty id /
// zero seq mean the corresponding field is absent.
func (c *Check) PreviousTxn() (string, uint32) {
	var id string
	var seq uint32
	if c.present&checkBitPreviousTxnID != 0 {
		id = c.PreviousTxnID
	}
	if c.present&checkBitPreviousTxnLgrSeq != 0 {
		seq = c.PreviousTxnLgrSeq
	}
	return id, seq
}

// ToMap returns the canonical JSON-map representation of the receiver,
// suitable for binarycodec.EncodeBytes. Includes every present field —
// metadata-excluded fields (sMD_Never) too — plus the LedgerEntryType
// header that every SLE blob carries.
func (c *Check) ToMap() map[string]any {
	out := map[string]any{
		"LedgerEntryType": "Check",
	}
	if c.present&checkBitAccount != 0 {
		out["Account"] = c.Account
	}
	if c.present&checkBitDestination != 0 {
		out["Destination"] = c.Destination
	}
	if c.present&checkBitSendMax != 0 {
		out["SendMax"] = c.SendMax
	}
	if c.present&checkBitSequence != 0 {
		out["Sequence"] = c.Sequence
	}
	if c.present&checkBitOwnerNode != 0 {
		out["OwnerNode"] = c.OwnerNode
	}
	if c.present&checkBitDestinationNode != 0 {
		out["DestinationNode"] = c.DestinationNode
	}
	if c.present&checkBitExpiration != 0 {
		out["Expiration"] = c.Expiration
	}
	if c.present&checkBitInvoiceID != 0 {
		out["InvoiceID"] = c.InvoiceID
	}
	if c.present&checkBitSourceTag != 0 {
		out["SourceTag"] = c.SourceTag
	}
	if c.present&checkBitDestinationTag != 0 {
		out["DestinationTag"] = c.DestinationTag
	}
	if c.present&checkBitFlags != 0 {
		out["Flags"] = c.Flags
	}
	if c.present&checkBitPreviousTxnID != 0 {
		out["PreviousTxnID"] = c.PreviousTxnID
	}
	if c.present&checkBitPreviousTxnLgrSeq != 0 {
		out["PreviousTxnLgrSeq"] = c.PreviousTxnLgrSeq
	}
	return out
}

// Encode serializes the receiver to canonical XRPL binary. Round-trip
// invariant: Decode(data); Encode() == data for any byte sequence that
// Decode accepts.
func (c *Check) Encode() ([]byte, error) {
	return binarycodec.EncodeBytes(c.ToMap())
}

// Hash returns the SHAMap account-state leaf hash for this entry,
// sha512Half(HashPrefixLeafNode || encoded || index). index is the
// 32-byte keylet under which the entry is stored.
func (c *Check) Hash(index [32]byte) ([32]byte, error) {
	data, err := c.Encode()
	if err != nil {
		return [32]byte{}, err
	}
	prefix := protocol.HashPrefixLeafNode
	return common.Sha512Half(prefix[:], data, index[:]), nil
}
